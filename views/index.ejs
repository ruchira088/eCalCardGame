<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= player.name %></p>
    <% for(var i = 0; i < player.cards.length; i++) {%>
    <img height="145" width="100" src="<%= player.cards[i].getPicture() %>">
    <% }%>
  </body>
</html>
